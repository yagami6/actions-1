name: My second Workflow
on: push
jobs:
    first_job:
         runs-on: ubuntu-latest
         steps:
            - name: Welcom message
              run: echo "hello world"
            - name: Read file
              run: cat README.md